using System;

namespace GeometryExample
{
    class Program
    {
        static void Main(string[] args)
        {
            // Create an instance of the Cylinder class
            Cylinder cylinder = new Cylinder();

            // Call the Process method to calculate
            cylinder.Process();

            // Call the Result method to display the results
            cylinder.Result();
        }
    }
}
